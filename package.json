{
  "name": "mcp-shared-memory-server",
  "version": "0.1.0",
  "description": "Local MCP-like server providing shared memory and agent linking via WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
