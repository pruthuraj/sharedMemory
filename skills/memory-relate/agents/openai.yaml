interface:
  display_name: "Memory Relate"
  short_description: "Connect memory graph nodes"
  default_prompt: "Use $memory-relate to connect two memory entries."
dependencies:
  tools:
    - type: "mcp"
      value: "shared-memory"
      description: "SharedMemory MCP server with memory_relate."
      transport: "stdio"
policy:
  allow_implicit_invocation: true
